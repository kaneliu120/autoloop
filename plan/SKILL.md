---
name: autoloop-plan
description: >
  AutoLoop 向导式任务配置。交互式收集任务参数，生成autoloop-plan.md。
  Use when: "autoloop plan", "配置任务", "创建计划".
---

# AutoLoop Plan

Read and follow the detailed execution protocol in `../commands/autoloop-plan.md`.

For shared protocols (quality gates, loop protocol, parameters, etc.), reference `../protocols/`.

For the full AutoLoop overview and core mechanisms, see `../SKILL.md`.
