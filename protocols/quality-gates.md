# Quality Gates — 质量门禁规范

## 概述

质量门禁是 AutoLoop 迭代的唯一终止标准。本文档定义所有任务类型的质量门禁，包括计算方法、评分规则和通过标准。

**核心原则**：质量门禁必须是**数字化的、可测量的**，不接受"差不多好了"这样的描述。

---

## 知识类任务门禁（T1/T2/T3/T4）

### 覆盖率（Coverage）

**定义**：已经有实质内容的维度数 / 计划调研的总维度数

**计算方法**：
```
已覆盖维度 = 在 findings.md 中有至少 3 个具体信息点（不是"未找到"）的维度数
覆盖率 = 已覆盖维度 / 总维度数 × 100%
```

**通过标准**：
- T1 Research：≥ 85%
- T2 Compare：100%（所有选项的所有维度都必须有内容）
- T4 Generate：每个生成单元都完成 = 100%

**示例计算**：
```
计划维度：8 个
有实质内容：功能(3个来源) + 性能(4个来源) + 成本(2个来源) + 社区(5个来源) + 成熟度(2个来源) = 5个
信息不足：学习曲线(1个来源) + 文档(0个来源) + 风险(1个来源) = 3个
覆盖率 = 5/8 = 62.5%（未达标）
```

---

### 可信度（Credibility）

**定义**：有 ≥ 2 个独立来源支撑的关键发现 / 总关键发现数

**什么是"独立来源"**：
- 同一作者不同文章 → 不算独立
- 同一公司不同产品页 → 不算独立
- 同一论坛不同帖子 → 不算独立（除非作者不同且有原创内容）
- 官方文档 + 第三方评测 → 算独立
- 不同媒体报道 + 官方数据 → 算独立

**信息来源可信度分级**：
```
级别 1（高可信）：官方文档/官方 GitHub/一手研究报告
级别 2（中高）：Gartner/Forrester/IDC/Stack Overflow/Hacker News 高票
级别 3（中）：知名技术媒体、专家博客（有实名认证）
级别 4（低中）：一般博客、Reddit 普通回答
级别 5（低）：匿名内容、未注明时间的内容
```

**计算方法**：
```
关键发现 = 在 findings.md 中明确标注为"关键结论"的陈述
有多源支撑 = 该陈述有 ≥2 个独立来源，且至少一个是级别 1-2
可信度 = 有多源支撑的关键发现 / 总关键发现 × 100%
```

**通过标准**：≥ 80%

---

### 一致性（Consistency）

**定义**：无内部矛盾的陈述数 / 总陈述数

**什么算矛盾**：
- 同一属性的不同数据（A 说性能 100ms，B 说 500ms，且无解释）
- 逻辑矛盾（优势和劣势中说了同一件事）
- 时间范围不明确导致的明显差异

**什么不算矛盾**（需要在 findings 中说明）：
- 不同使用场景下的不同表现
- 不同时间版本的不同结果
- 主观评价的差异

**计算方法**：
```
矛盾维度 = 在 cross-verification 报告中标注为"存在矛盾"的维度数
一致性 = 1 - (矛盾维度 / 总维度) × 100%
```

**通过标准**：≥ 90%

---

### 完整性（Completeness）

**定义**：有引用来源的关键陈述数 / 总关键陈述数

**关键陈述 vs 一般陈述**：
- 关键陈述：用于支撑结论或推荐的陈述（必须有来源）
- 一般陈述：背景描述、解释性内容（建议有来源但不强制）

**计算方法**：
```
关键陈述 = 在 findings.md 中的结论类、数据类、推荐依据类陈述
有来源 = 陈述后有 (来源: URL) 或 [^N] 引用标注
完整性 = 有来源的关键陈述 / 总关键陈述 × 100%
```

**通过标准**：≥ 85%

---

## 工程类任务门禁（T5/T6/T7）

### 语法验证门禁（强制，阶段 1 结束前）

```bash
# 所有修改的 Python 文件必须通过
python3 -m py_compile {文件路径}

# 所有修改的 TypeScript 文件必须通过
cd {前端目录} && npx tsc --noEmit

# 通过标准：零错误（有 warning 可以接受，但必须记录）
```

### 路由注册门禁（强制，有新路由时）

```bash
# 验证新路由已注册
grep -n "include_router\|{路由函数名}" {main.py 路径}

# 通过标准：grep 找到注册语句
```

### 安全性门禁（T5/T6）

| 严重级别 | 问题类型 | 通过标准 |
|---------|---------|---------|
| P1（阻塞） | SQL注入、命令注入、敏感数据暴露 | = 0 个 |
| P2（阻塞） | XSS、路径穿越、缺少输入验证 | = 0 个 |
| P3（记录） | CORS 配置、过宽的错误消息 | 记录，不阻塞 |

T6 企业级标准：安全性得分 ≥ 9/10

### 可靠性门禁（T5/T6）

| 严重级别 | 问题类型 | 通过标准 |
|---------|---------|---------|
| P1（阻塞） | 静默失败 (except: pass)、关键路径无异常处理 | = 0 个 |
| P2（阻塞） | 无超时配置、缺少降级回退 | = 0 个（T6）|
| P3（记录） | 日志不完整、重试逻辑缺失 | 记录，不阻塞 |

T6 企业级标准：可靠性得分 ≥ 8/10

### 可维护性门禁（T5/T6）

| 严重级别 | 问题类型 | 通过标准 |
|---------|---------|---------|
| P1（阻塞） | 新路由未注册、新文件未导出 | = 0 个 |
| P2（建议）| `any` 类型滥用、重复代码 | ≤ 3 个（T5）/ = 0 个（T6）|
| P3（记录） | 命名不规范、注释缺失 | 记录，不阻塞 |

T6 企业级标准：可维护性得分 ≥ 8/10

### 架构门禁（T7）

| 问题类型 | 通过标准 |
|---------|---------|
| 循环依赖 | = 0 个 |
| 跨层直接访问 | = 0 个（路由层直接操作 DB）|
| API 不一致（同类路由格式不同）| ≤ 2 处（记录，计划重构）|

T7 企业级标准：架构得分 ≥ 8/10

### 性能门禁（T7）

| 问题类型 | 通过标准 |
|---------|---------|
| N+1 查询 | = 0 个（生产路径）|
| 连接池配置缺失 | = 0 个 |
| 热路径无缓存（高频读，低更新）| ≤ 2 处（记录，计划添加）|

T7 企业级标准：性能得分 ≥ 8/10

### 稳定性门禁（T7）

| 问题类型 | 通过标准 |
|---------|---------|
| 外部依赖无降级 | = 0 个（Redis / 第三方 API）|
| 健康检查端点缺失 | = 0 个 |
| 关键操作无超时 | = 0 个 |

T7 企业级标准：稳定性得分 ≥ 8/10

---

## 门禁评估矩阵（快速查阅）

| 模板 | 门禁 1 | 门禁 2 | 门禁 3 | 门禁 4 | 阻塞条件 |
|------|--------|--------|--------|--------|---------|
| T1 Research | 覆盖率 ≥ 85% | 可信度 ≥ 80% | 一致性 ≥ 90% | 完整性 ≥ 85% | 任一未达标 |
| T2 Compare | 覆盖率 100% | 可信度 ≥ 80% | 偏见检查通过 | 敏感性分析完成 | 任一未达标 |
| T3 Iterate | KPI 达目标值 | — | — | — | KPI 未达标 |
| T4 Generate | 通过率 ≥ 95% | 平均分 ≥ 7/10 | — | — | 任一未达标 |
| T5 Deliver | py_compile 通过 | P1/P2 = 0 | 4 服务全 active | 线上验收通过 | 任一未达标 |
| T6 Quality | 安全 ≥ 9/10 | 可靠 ≥ 8/10 | 可维护 ≥ 8/10 | — | 任一未达标 |
| T7 Optimize | 架构 ≥ 8/10 | 性能 ≥ 8/10 | 稳定 ≥ 8/10 | — | 任一未达标 |

---

## 门禁豁免规则

在以下情况可以记录豁免（不是跳过，是明确说明为什么可以接受）：

1. **遗留代码豁免**：不在本次修改范围内的遗留问题，可以记录但不强制修复
2. **技术债豁免**：P3 级别的问题，在预算耗尽时可以记录到"遗留问题"，不阻塞交付
3. **环境限制豁免**：线上验收因环境问题无法执行的步骤，需要说明替代验证方法

**豁免必须**：
- 明确说明豁免原因
- 评估影响（是否影响核心功能）
- 记录到 progress.md 和最终报告的"遗留问题"部分
- 不得隐藏（不是"忘了"，是"知道了，决定不修复，原因是..."）
